/**
 * localStorage keys the Flow workspace shipped with. They keep their original
 * names so existing users do not silently lose what they saved.
 */
export const FLOW_GLOBAL_CONSTRAINTS_KEY = "next-ai-draw-io-global-constraints"
