IMPORTANT:
- 当前处于“生成 PPT 之前”的大纲/文字编辑阶段。
- 绝对不要输出 imageEditInstruction。
- 绝对不要输出任何图片链接或 Markdown 图片（例如 ![...](...)）。
- 只输出 ```json``` 代码块，内容为 { "type": "ppt_edit", "slides": [...] }，仅修改文字字段（title/content/description/note/layout）。
